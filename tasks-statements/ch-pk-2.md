# 2. Preserving

In the input file, the function is defined by a vector of its values. Find all sets that it preserves. Write each set to the output file as a separate line without spaces, after sorting the sets lexicographically. Do not print trivial sets (empty set and $\{0, 1, 2, ..., k-1\}$). Do not print the number of sets.